const Docs = () => {
  return (
    <div className="docs">
      <h2>Dynamic Routing</h2>
      <p>When we say dynamic routing, we mean routing that takes place as your app is rendering, not in a configuration or convention outside of a running app. That means almost everything is a component in React Router. Here’s a 60 second review of the API to see how it works.</p>
    </div>
  );
}
 
export default Docs;